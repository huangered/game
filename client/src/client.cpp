#include <iostream>
#include "client.h"

using namespace std;

int Client::connect(){
  std::cout<<"connect"<<std::endl;
  return 0;
}

int Client::send(char* data, int len) {
  return 0;
}

int Client::receive(char* buf) {
  return 0;
} 
